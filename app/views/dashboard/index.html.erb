<div>
  <h3 class="text-lg font-medium leading-6 text-gray-900">
    Last 30 days
  </h3>
  <dl class="grid grid-cols-1 gap-5 mt-5 sm:grid-cols-3">
    <div class="px-4 py-5 overflow-hidden bg-white rounded-lg shadow sm:p-6">
      <dt class="text-sm font-medium text-gray-500 truncate">
        Total Members
      </dt>
      <dd class="mt-1 text-3xl font-semibold text-gray-900">
        <%#= number_with_delimiter(current_corporation.members.count) %>
      </dd>
    </div>

    <div class="px-4 py-5 overflow-hidden bg-white rounded-lg shadow sm:p-6">
      <dt class="text-sm font-medium text-gray-500 truncate">
        EVEmails Delivered
      </dt>
      <dd class="mt-1 text-3xl font-semibold text-gray-900">
        <%#= number_with_delimiter(current_corporation.deliveries.where(status: 'delivered', delivered_at: 30.days.ago..Date.tomorrow).count) %>
      </dd>
    </div>

    <div class="px-4 py-5 overflow-hidden bg-white rounded-lg shadow sm:p-6">
      <dt class="text-sm font-medium text-gray-500 truncate">
        Scheduled Deliveries
      </dt>
      <dd class="mt-1 text-3xl font-semibold text-gray-900">
        <%#= number_with_delimiter(current_corporation.deliveries.where(status: 'scheduled').count) %>
      </dd>
    </div>
  </dl>
</div>
